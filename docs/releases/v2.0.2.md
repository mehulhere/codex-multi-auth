## Core

### Improvements

- Device-code login tokens persist through existing account storage, Codex CLI sync, and active-account state.

### Bugfixes

- Device-code poll responses are kept out of logs and account storage.

## CLI

### Improvements

- Added `codex auth login --device-auth` for headless and remote shells using device-code polling and PKCE token exchange through the Codex device redirect URI.

### Bugfixes

- Rejected `--device-auth` combined with `--manual` or `--no-browser`; browser-first, manual paste, and no-browser paths are unchanged.
