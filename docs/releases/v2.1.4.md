## Bug Fixes
- Fixed Codex TUI goal proxy handling for snake-case `thread_id` request bodies and local blocked-goal fallback responses. (#462)
- Fixed runtime handling for workspace-disabled and deactivated accounts so account state remains disabled and visible instead of being hidden by goal fallback behavior. (#463)
- Fixed model-matrix release validation so unsupported current-account capabilities are skipped by default and terminal JSON failures cannot be followed by false-green echoed prompt tokens. (#462, #463)

## Chores
- Serialized Vitest release scripts around the known Windows fake-timer runtime-toast timeout and expanded focused runtime proxy/model-capability regression coverage. (#462, #463)
- Validated the release with build, typecheck, script typecheck, lint, full tests, coverage, model matrix, vendor provenance, repo hygiene, package budget, and whitespace checks.

## Changelog

Full Changelog: https://github.com/ndycode/codex-multi-auth/compare/v2.1.3...v2.1.4

- #462 Fix Codex TUI goal proxy handling @ndycode
- #463 Fix runtime skip for deactivated accounts @ndycode
