## Auth

### Bugfixes

- Stabilized `codex auth switch <index>` + host auth sync so local account selection stays deterministic while sync failures are reported clearly.
- Hardened refresh token normalization and refresh queue stale/timeout recovery to reduce stuck lanes and duplicate refresh churn.

### Tests

- Expanded deterministic regression coverage across auth, refresh queue, docs integrity, retry/backoff, and CLI routing paths.
- Added script-level audit allowlist regression coverage for advisory-source edge cases and Windows `cmd.exe` execution path.
