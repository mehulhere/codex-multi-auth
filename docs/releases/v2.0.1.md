## New Features
- Enabled runtime rotation by default for installed wrapper users while keeping `codex auth rotation disable`, `codexRuntimeRotationProxy=false`, and `CODEX_MULTI_AUTH_RUNTIME_ROTATION_PROXY=0` opt-outs. (#439)
- Added install/update self-healing for packaged Codex app bind and supported user-level app launcher routing when runtime rotation is enabled. (#439)
- Added best-effort package auto-update checks for installed packages outside CI, test, and local worktree contexts. (#439)
- Updated live quota probes and fallback model defaults to current OpenAI/Codex selectors including `gpt-5.3-codex` and `gpt-5.5`. (#439)

## Chores
- Validated default runtime rotation, app-bind install/update behavior, auto-update coverage, documentation integrity, typecheck, lint, build, full tests, and dry-run packaging. (#439)

## Changelog

Full Changelog: https://github.com/ndycode/codex-multi-auth/compare/v2.0.0...v2.0.1

- #439 Enable runtime rotation and auto-update by default @ndycode
