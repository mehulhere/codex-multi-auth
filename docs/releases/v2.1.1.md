## Bug Fixes
- Fixed local governance command routing so `codex auth usage`, `account`, `budget`, `bridge`, `integrations`, `models`, and `monitor` stay in the local multi-auth handler instead of falling through to official Codex.
- Fixed `codex auth bridge token list --json` to return valid JSON in an empty token-store state and keep token hashes out of list output.
- Stabilized retry-suite timers after the local governance routing repair.

## Chores
- Verified the patch with focused bridge/routing suites, local-bridge and integration command suites, lint, typecheck, build, and an installed local CLI command matrix.

## Changelog

Full Changelog: https://github.com/ndycode/codex-multi-auth/compare/v2.1.0...v2.1.1

- bb7a8aa fix(release): repair local governance command routing @ndycode
- 658a0d1 test: stabilize retry suite timers @ndycode
